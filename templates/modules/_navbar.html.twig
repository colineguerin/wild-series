<nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand text-light px-4" href="/">Wild Series</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item px-3">
                    <a class="nav-link text-light" href="/program">Programs</a>
                </li>
                <li class="nav-item px-3">
                    <a class="nav-link text-light" href="/actor">Actors</a>
                </li>
                {{ component('NavbarComponent') }}
            </ul>
            <div>
                {% if app.user %}
                    {{ app.user.email }}
                    <button class="btn btn-primary"><a href="{{ path('app_logout') }}" class="text-decoration-none text-light">Se déconnecter</a></button>
                {% else %}
                    <button class="btn btn-primary"><a href="{{ path('app_login') }}" class="text-decoration-none text-light">Se connecter</a></button>
                {% endif %}
            </div>
        </div>
    </div>
</nav>